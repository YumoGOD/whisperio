APP_NAME=Локальный транскрибатор
APP_ENV=local
APP_HOST=0.0.0.0
APP_PORT=8000

DATA_DIR=/app/data
UPLOAD_DIR=/app/data/uploads
WORK_DIR=/app/data/work
TRANSCRIPT_DIR=/app/data/transcripts
LOG_DIR=/app/data/logs
GLOSSARY_DIR=/app/data/glossary
DATABASE_PATH=/app/data/transcriber.db

MAX_UPLOAD_MB=5120
ALLOWED_EXTENSIONS=*

DEFAULT_PROFILE=accuracy_first
WHISPER_MODEL=large-v3
# После скачивания модели — локальный путь к CTranslate2 (без загрузки из сети):
# WHISPER_MODEL=/app/data/models/faster-whisper-large-v3
# Устройство: cuda (GPU), cpu, auto
WHISPER_DEVICE=cuda
# float16 — стандарт для GPU; int8_float16 — меньше VRAM, чуть медленнее; int8 — для CPU
WHISPER_COMPUTE_TYPE=float16
# Число воркеров загрузки данных внутри WhisperModel (обычно 1 для GPU)
WHISPER_NUM_WORKERS=1
WHISPER_DOWNLOAD_ROOT=/app/data/models
WHISPER_LANGUAGE=ru
WHISPER_TASK=transcribe

# На одной GPU обычно 1
WORKER_CONCURRENCY=1
WORKER_POLL_SECONDS=5
WORKER_STALE_RUNNING_MINUTES=30
WORKER_ID=

CHUNK_SECONDS=1800
CHUNK_OVERLAP_SECONDS=15
ENABLE_LOUDNORM=true
TARGET_SAMPLE_RATE=16000

# VAD (Voice Activity Detection) — Silero VAD внутри faster-whisper.
# Оставлен ВЫКЛЮЧЕН по умолчанию: аудио с камер наблюдения часто содержит тихую речь
# на фоне постоянного шума (вентиляция, гул), и VAD может её срезать.
# Для нормализации тихого аудио используй ENABLE_LOUDNORM=true (включён выше) —
# это предпочтительный подход для плохого аудио.
#
# Если включить VAD (VAD_FILTER=true), рекомендуемые настройки для тихой речи:
#   VAD_THRESHOLD=0.25      — ниже = чувствительнее (дефолт Silero = 0.5, текущий = 0.35)
#   VAD_SPEECH_PAD_MS=800   — больше паддинга вокруг речевых сегментов
#   VAD_MIN_SILENCE_MS=1200 — минимальная пауза для разреза (текущее значение оставить)
VAD_FILTER=false
VAD_MIN_SILENCE_MS=1200
VAD_SPEECH_PAD_MS=600
VAD_THRESHOLD=0.35

GLOSSARY_PATH=/app/data/glossary/global.yml
GLOSSARY_PROMPT_MAX_CHARS=700
GLOSSARY_CONTEXT_MAX_CHARS=1200
GLOSSARY_HOTWORDS_MAX=80
GLOSSARY_ENABLE_HOTWORDS=false
GLOSSARY_ENABLE_HARD_NORMALIZATION=true
GLOSSARY_REPETITION_COMPRESSION_THRESHOLD=4.0
